#include<stdio.h>
int Doinb(int n)
{
	int peach = 1;
	int i;
	for (i = 0; i < n; i++)
		peach = (peach+1)* (peach+1);
	return peach;
}
void main()
{
	int n;
	int peach;
	printf("天数\n");
	scanf_s("%d", &n);
	peach = Doinb(n);
	printf("doinb在第一天摘下的桃子数量为%d个\n", peach);
}
