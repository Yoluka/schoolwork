#include<stdio.h>
int main()
{
	int a[20];
	int i = 0;
	while (scanf_s("%d", &a[i++]))
	{
		if (a[i - 1] == 0)
			break;
	}
	for (int j = i - 2; j >= 0; j--)
		printf("%d", a[j]);
	printf("\n");
	return 0;
}
